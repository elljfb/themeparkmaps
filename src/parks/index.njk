---
layout: layouts/base.njk
title: Parks
description: Browse theme park map collections by destination.
---

<section class="page-hero compact-hero">
  <div class="container">
    <h1>Popular Parks</h1>
    <p>Start with the parks that defined eras of paper guide maps, holiday planning, and walk-through-the-turnstiles nostalgia.</p>
  </div>
</section>

<section class="content-band">
  <div class="container park-directory-grid">
    {% for park in parks %}
      <article class="park-directory-card" id="{{ park.slug }}">
        <a href="/maps/">
          <div class="park-card-image-wrapper">
            <img src="{{ park.hero }}" alt="{{ park.name }} placeholder image">
            <span class="park-card-count">{{ park.count }} maps</span>
          </div>
          <div class="park-card-content">
            <span class="eyebrow">{{ park.location }}</span>
            <h2>{{ park.name }}</h2>
            <p>{{ park.summary }}</p>
            <span class="park-card-btn">Browse Maps</span>
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
</section>
