{
  "name": "themeparkmaps",
  "version": "1.0.0",
  "private": true,
  "description": "ThemeParkMaps.net static website built with Eleventy.",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --watch",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "rimraf": "^6.0.1"
  }
}
